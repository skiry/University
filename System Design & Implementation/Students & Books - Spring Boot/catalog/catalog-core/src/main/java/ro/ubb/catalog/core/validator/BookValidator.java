package ro.ubb.catalog.core.validator;

import ro.ubb.catalog.core.model.Book;

public class BookValidator implements Validator<Integer, Book> {
    public void validate( Book book ) throws ValidatorException {
        // Nothing so far.
    }
}
